# Copy to .env and adjust (see .gitignore — .env is not committed)

NODE_ENV=development
PORT=3000

# SQLite database file path (relative to process cwd or absolute)
DATABASE_FILE=db.sqlite

# true | false | empty (empty = auto: false in production, true otherwise)
DB_SYNCHRONIZE=
